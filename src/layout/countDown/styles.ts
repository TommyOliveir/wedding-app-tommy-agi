import { styled } from "@mui/system";

export const CountdownStyled = styled("div")({
  fontSize: "1.8em",
  color: "#40695bff",
  fontWeight: "bold",
  "@media (min-width: 600px)": { fontSize: "4em" },
});
